#include <stdio.h>

/**
* main - prints a single numbers
*
* Return: Always
*
*/
int main(void)
{
	int numbers;

	for (numbers = 0; numbers < 10; numbers++)
	{
	printf("%d", numbers);
	}
putchar('\n');
return (0);
}

